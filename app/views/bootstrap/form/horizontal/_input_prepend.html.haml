.control-group{:class => state}
  = builder.label method, :class => 'control-label'

  .controls
    .input-prepend
      %span.add-on
        = prepend

      = yield
      - if help
        %span.help-block
          = help
