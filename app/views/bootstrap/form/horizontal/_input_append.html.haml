.control-group{:class => state}
  = builder.label method, :class => 'control-label'

  .controls
    .input-append
      = yield
      %span.add-on
        = append
      - if help
        %span.help-block
          = help
